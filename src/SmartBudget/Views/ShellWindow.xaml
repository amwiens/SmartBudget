<Window x:Class="SmartBudget.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="{StaticResource BrushOutline}"
                BorderThickness="0 0 1 0">
            <!--<TextBlock Text="SmartBudget" />-->
            <ContentControl prism:RegionManager.RegionName="Sidebar" />
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    BorderBrush="{StaticResource BrushOutline}"
                    BorderThickness="0 0 0 1">
                <ContentControl prism:RegionManager.RegionName="Topbar" />
            </Border>

            <ContentControl Grid.Row="1"
                            prism:RegionManager.RegionName="Content" />
        </Grid>
    </Grid>
</Window>